import assert from 'node:assert';
{
  assert(counter += 1);
}
{
  assert(dog.age += 1);
}
{
  assert(dog.age    +=  1);
}
{
  assert(dog.age += 1 === three);
}
{
  assert([x] = [3]);
}
{
  assert([x] = [foo]);
}

