{
  "name": "@power-assert/transpiler",
  "version": "0.0.0",
  "description": "power-assert transpiler",
  "main": "dist/transpile-with-sourcemap.mjs",
  "scripts": {
    "build:clean": "rimraf dist && rm tsconfig.tsbuildinfo",
    "lint": "eslint src --ext .mts",
    "fix": "npm run lint:src -- --fix"
  },
  "exports": {
    ".": "./dist/transpile-with-sourcemap.mjs",
    "./package.json": "./package.json",
    "./src/transpile-with-sourcemap.mjs": "./dist/transpile-with-sourcemap.mjs"
  },
  "keywords": [
    "power-assert"
  ],
  "author": "Takuto WADA",
  "license": "MIT",
  "dependencies": {
    "@power-assert/transpiler-core": "^0.0.0",
    "acorn": "^8.10.0",
    "astring": "^1.8.6",
    "convert-source-map": "^2.0.0",
    "multi-stage-sourcemap": "^0.3.1",
    "source-map": "^0.7.4"
  }
}
